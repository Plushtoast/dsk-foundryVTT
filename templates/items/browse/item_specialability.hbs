{{#if item.system.requirements}}
<div class="row-section tooltip-row">
    <div class="col four label-text">{{localize "dsk.requirements"}}</div>
    <div class="col six">{{item.system.requirements}}</div>
</div>
{{/if}}
