{{#if item.system.traitType}}
<div class="row-section tooltip-row">
    <div class="col four label-text">{{localize "dsk.traitType"}}</div>
    <div class="col six">{{item.system.traitType}}</div>
</div>
{{/if}}
{{#if item.system.tp}}
<div class="row-section tooltip-row">
    <div class="col four label-text">{{localize "dsk.CHARAbbrev.damage"}}</div>
    <div class="col six">{{item.system.tp}}</div>
</div>
{{/if}}
{{#if item.system.at}}
<div class="row-section tooltip-row">
    <div class="col four label-text">{{localize "dsk.ABBR.AW"}}</div>
    <div class="col six">{{item.system.at}}</div>
</div>
{{/if}}
